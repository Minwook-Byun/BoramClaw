# BoramClaw 자연어 인터페이스 데모

## 🎯 핵심: 이미 자연어로 동작합니다!

BoramClaw는 Claude API 기반이므로, **자연어 질문이 자동으로 적절한 툴을 호출**합니다.

## 💬 실제 대화 예시

### 데모 1: 현재 활동 조회

```
User: 뭐하니 너 지금?
User: 지금 무엇 하고 있어?
User: What am I doing now?
User: 현재 작업 상태 알려줘

→ Claude가 자동 판단: get_current_context 툴 호출
→ 결과: "현재 Python 개발 작업 중입니다. 최근 30분간 python3 명령어를 25회 실행..."
```

### 데모 2: 일일 활동 조회

```
User: 오늘 뭐 했지?
User: 오늘 개발 기록 보여줘
User: Today's activity report
User: 오늘 생산성이 어땠어?

→ Claude가 자동 판단: workday_recap {"mode":"daily"} 툴 호출
→ 결과: "오늘 활동: Shell 296개 명령어, Browser 19개 페이지..."
```

### 데모 3: 주간 활동 조회

```
User: 일주일 전에 뭐했더라?
User: 이번 주 뭐 했어?
User: This week's progress?
User: 주간 리포트 보여줘

→ Claude가 자동 판단: workday_recap {"mode":"weekly"} 툴 호출
→ 결과: "이번 주 활동: 커밋 15개, 명령어 2,000개..."
```

### 데모 4: 특정 활동 조회

```
User: 유튜브에서 뭐 봤어?
User: 어제 웹에서 뭐 검색했더라?
User: 최근에 GitHub 많이 봤어?

→ Claude가 자동 판단: browser_research_digest 툴 호출
→ 결과: "YouTube 17개 영상 시청, GitHub 방문 5회..."
```

### 데모 5: Git 활동 조회

```
User: 오늘 커밋 몇 개 했어?
User: 최근 코딩 얼마나 했지?
User: 어떤 파일 수정했더라?

→ Claude가 자동 판단: git_daily_summary 툴 호출
→ 결과: "커밋 3개, 변경 파일 5개, +120 -50 라인..."
```

### 데모 6: Shell 활동 조회

```
User: 오늘 어떤 명령어 많이 썼어?
User: 자주 쓰는 명령어 알려줘
User: 반복되는 명령어 있어?

→ Claude가 자동 판단: shell_pattern_analyzer 툴 호출
→ 결과: "python3 25회, git 10회, Alias 추천..."
```

## 🎬 실제 시연

### Interactive Mode 실행

```bash
python3 main.py
```

대화 시작:
```
🤖 BoramClaw: 안녕하세요! 무엇을 도와드릴까요?

You: 뭐하니 너 지금?

🤖 BoramClaw: [get_current_context 툴 호출 중...]

현재 개발 작업 중입니다.

📊 최근 30분 활동:
- Python 명령어 25회 실행
- Git 명령어 10회 실행
- VS Code에서 context_engine.py 편집 중

주요 활동: Development (신뢰도 80%)

You: 오늘 뭐 했지?

🤖 BoramClaw: [workday_recap 툴 호출 중...]

📊 오늘 개발 활동 리포트

✨ 오늘 활동: 명령어 296개, 웹 방문 19개

### 💻 Shell 활동
- 명령어 실행: 296개 (유니크: 143개)
- 자주 쓴 명령어:
  • python3: 25회
  • git: 10회
  • npm: 11회

### 🌐 Browser 활동
- 방문: 19개 페이지
- 자주 방문한 도메인:
  • youtube.com: 17회
  • google.com: 2회

You: 유튜브에서 뭐 봤어?

🤖 BoramClaw: [browser_research_digest 툴 호출 중...]

YouTube에서 다음 영상들을 시청하셨습니다:

1. "OpenClaw와 2026년 2월의 신호들"
2. "오픈클로, 왜 이 기술이 AI 판을 뒤집었나?"
3. "Claude Sonnet 4.5 발표"
...

총 17개 영상 시청
```

## 🔥 더 자연스러운 질문도 가능

### 복합 질문

```
User: 오늘 생산성이 어땠는지 분석해줘

→ Claude가 자동으로:
1. workday_recap 호출
2. git_daily_summary 호출
3. 결과 종합 분석
→ "오늘 커밋 3개, 명령어 296개 실행. 활발한 개발 활동..."
```

### 시간 범위 질문

```
User: 지난 3일간 뭐 했어?

→ Claude가 자동으로 days=3 설정하여 호출
→ 3일간 통합 리포트
```

### 비교 질문

```
User: 이번 주가 지난 주보다 생산적이었어?

→ Claude가 자동으로:
1. 이번 주 리포트
2. 지난 주 리포트 (파일에서 읽기)
3. 비교 분석
```

## 🎯 왜 이게 OpenClaw와 다른가?

### OpenClaw (메시징 허브)
```
User: 슬랙에 메시지 보내줘
→ Slack API 호출
```

### BoramClaw (컨텍스트 엔진)
```
User: 뭐하니 너 지금?
→ 4개 데이터 소스 실시간 분석
→ 지능적 컨텍스트 파악
→ 자동 답변
```

**핵심 차이:**
- OpenClaw: 메시지 라우팅
- BoramClaw: **행동 관찰 + 맥락 이해**

## 🚀 고급 기능: 대화형 디버깅

```
User: 왜 커밋이 적어?

🤖 BoramClaw: [Context 분석]
최근 3시간 동안 커밋이 없습니다.
Shell 활동은 활발한데 (python3 25회),
Git 명령어는 10회만 실행되었습니다.

분석 결과:
- 개발은 활발하게 진행 중
- 커밋을 자주 하지 않는 패턴
- 권장: 작업 단위로 자주 커밋하세요

User: 그럼 지금 커밋해야 할까?

🤖 BoramClaw: [Git 상태 확인]
변경된 파일 5개 (context_engine.py, rules_engine.py...)
+280 라인 추가, -50 라인 삭제

네, 커밋하기 좋은 시점입니다!
메시지 추천: "Phase 4-5 완료: Context Engine + Rules Engine 구현"
```

## 💡 실전 활용

### 시나리오 1: 아침 출근

```
User: 어제 뭐까지 했더라?

→ 어제 리포트 자동 생성
→ "어제는 MCP Server 구현까지 완료..."
```

### 시나리오 2: 주간 회의 준비

```
User: 이번 주 개발 진행상황 요약해줘

→ 주간 리포트 + 자동 분석
→ "이번 주 커밋 15개, 주요 작업: Phase 1-5 완료..."
```

### 시나리오 3: 집중 시간 확인

```
User: 오늘 몇 시간 코딩했어?

→ Session detection + 분석
→ "총 5시간 30분, 2개 세션 (3시간 + 2.5시간)"
```

## 🎨 커스텀 자연어 패턴

`main.py`에 이미 구현된 자연어 패턴들:

```python
# parse_arxiv_quick_request()
"arXiv 논문 요약해줘" → arxiv_daily_digest 호출

# parse_today_command()
"/today" 또는 자연어로 "오늘" → workday_recap 호출

# parse_week_command()
"/week" 또는 자연어로 "이번 주" → weekly recap

# parse_context_command()
"/context" 또는 자연어로 "지금" → get_current_context
```

## 🔧 더 강화하려면?

### 1. System Prompt 최적화

`main.py`의 시스템 프롬프트에 추가:

```python
SYSTEM_PROMPT = """
당신은 BoramClaw, 개발자의 모든 활동을 추적하는 AI 비서입니다.

사용자가 다음과 같이 질문하면 자동으로 적절한 툴을 호출하세요:

- "뭐하니", "지금", "현재" → get_current_context
- "오늘", "today" → workday_recap (daily)
- "이번 주", "week" → workday_recap (weekly)
- "유튜브", "웹 검색" → browser_research_digest
- "커밋", "git" → git_daily_summary
- "명령어", "shell" → shell_pattern_analyzer

자연스러운 대화로 답변하되, 항상 데이터에 기반하세요.
"""
```

### 2. 더 많은 패턴 추가

```python
# runtime_commands.py에 추가
def parse_natural_language_query(text: str) -> Optional[dict]:
    """자연어 쿼리를 툴 호출로 변환"""

    text_lower = text.lower()

    # "뭐하니", "지금"
    if any(word in text_lower for word in ["뭐하니", "지금", "현재", "what am i"]):
        return {"tool": "get_current_context", "input": {}}

    # "오늘"
    if any(word in text_lower for word in ["오늘", "today"]):
        return {"tool": "workday_recap", "input": {"mode": "daily"}}

    # "이번 주"
    if any(word in text_lower for word in ["이번 주", "week", "주간"]):
        return {"tool": "workday_recap", "input": {"mode": "weekly"}}

    return None
```

## ✅ 결론

**BoramClaw는 이미 완전한 자연어 인터페이스를 가지고 있습니다!**

- ✅ Claude API 기반 자연어 이해
- ✅ 자동 툴 선택 및 호출
- ✅ 대화형 응답
- ✅ 컨텍스트 유지

**CLI 명령어는 단지 빠른 실행을 위한 shortcut일 뿐!**

진짜 파워는 자연어 대화에 있습니다! 🚀
